# .github/workflows/daily-production.yml
# Workflow final optimisé pour la production quotidienne.

name: 🚀 Production - Lancement Quotidien du Moteur

# ===============================================
# DÉCLENCHEURS
# ===============================================
on:
  # 1. Planification Quotidienne (ex: minuit UTC)
  schedule:
    # Lance le workflow tous les jours à 00:00 UTC
    # Ajustez l'heure selon votre fuseau horaire et votre premier créneau de publication
    - cron: '0 0 * * *' 
  
  # 2. Lancement Manuel avec mode Debug
  workflow_dispatch:
    inputs:
      debug_mode:
        description: 'Activer le mode Debug (true/false) pour le script Python.'
        required: true
        default: 'false'
        type: boolean
        
# Permissions de sécurité minimales
permissions:
  contents: read # Lecture seule pour le checkout
  
jobs:
  daily_production_job:
    # Utilisation d'un conteneur Docker pour garantir la cohérence de l'environnement
    container:
      image: python:3.11-slim-buster # Image Python légère et stable
      
    runs-on: ubuntu-latest
    
    # ===============================================
    # VARIABLES D'ENVIRONNEMENT GLOBALES
    # ===============================================
    env:
      # DEBUG_MODE lu depuis l'input manuel ou 'false' par défaut pour la planification
      DEBUG_MODE: ${{ github.event.inputs.debug_mode || 'false' }} 
      ENGINE_SCRIPT: 'content_factory/auto_content_engine.py'
      # Pas besoin de DISABLE_SCRIPT ici si l'upload est toujours requis en PROD
      OUTPUT_ROOT: 'output' # Correspond à la configuration dans config.yaml
      
      # 🔑 INJECTION DES SECRETS DE PRODUCTION
      # Ces variables sont lues par le config_loader.py via le placeholder ${VARIABLE_NAME}
      YOUTUBE_CLIENT_ID: ${{ secrets.YOUTUBE_CLIENT_ID }}
      YOUTUBE_CLIENT_SECRET: ${{ secrets.YOUTUBE_CLIENT_SECRET }}
      YOUTUBE_REFRESH_TOKEN: ${{ secrets.YOUTUBE_REFRESH_TOKEN }}
      
    steps:
    
    # ⬇️ ÉTAPE 1: Initialisation
    - name: ⬇️ Checkout du code
      uses: actions/checkout@v4
      with:
        path: app # Le code sera cloné dans /app dans le conteneur
    
    # 📦 ÉTAPE 2: Installation des dépendances
    - name: 📦 Préparation de l'environnement
      working-directory: ./app # Répertoire de travail
      run: |
        echo "## Installation des dépendances Python"
        python -m pip install --upgrade pip setuptools
        
        # Installer les dépendances du projet
        # Nécessite: moviepy, google-api-python-client, google-auth, PyYAML, etc.
        pip install -r requirements.txt
        
        # Vérification critique de PyYAML pour le ConfigLoader
        pip show pyyaml
        
    # 🚀 ÉTAPE 3: Exécution du Moteur (Production)
    - name: 🚀 Lancement du Moteur de Contenu
      working-directory: ./app
      run: |
        echo "## Démarrage du processus de génération et d'upload"
        echo "Mode Debug: ${{ env.DEBUG_MODE }}"
        
        # Lancement de l'Engine. Le code de sortie du script (0 ou 1) déterminera
        # le succès ou l'échec de cette étape.
        # On passe le mode DEBUG en variable d'environnement au script python
        DEBUG_MODE=${{ env.DEBUG_MODE }} python "${{ env.ENGINE_SCRIPT }}"
          
    # 💾 ÉTAPE 4: Sauvegarde des artefacts (Vidéo finale, Audio, Images)
    # L'artefact est uploadé depuis le chemin de travail du conteneur (/app/output)
    - name: 💾 Sauvegarde des Artefacts de génération
      uses: actions/upload-artifact@v4
      if: always() # Toujours uploader pour analyse en cas d'échec
      with:
        name: resultats-${{ github.run_id }}
        path: app/${{ env.OUTPUT_ROOT }} 
        retention-days: 7 
        
    # 🔔 ÉTAPE 5: Notifications (avec statut clair)
    - name: 🔔 Notification de Succès
      if: success()
      run: |
        echo "🎉 Le workflow de production s'est terminé avec succès !"
        
    - name: 🔔 Notification d'Échec
      if: failure()
      run: |
        echo "❌ ÉCHEC CRITIQUE: Le workflow de production a échoué. Consulter l'artefact pour les détails."

