# .github/workflows/daily-production.yml
# Workflow final optimisé (Utilisation de Conteneur pour la stabilité)

name: 🚀 Production - Lancement Quotidien (Optimisé)

# Événement de lancement manuel avec mode Debug
on:
  workflow_dispatch:
    inputs:
      debug_mode:
        description: 'Activer le mode Debug (true/false) pour le script Python.'
        required: true
        default: 'false'
        type: boolean
        
# Permissions de sécurité minimales
permissions:
  contents: read # Lecture seule pour le checkout
  
jobs:
  daily_production_job:
    # Utilisation d'un conteneur Docker pour garantir la cohérence de l'environnement
    container:
      image: python:3.11-slim-buster # Image Python légère et stable
      
    runs-on: ubuntu-latest
    
    # Variables d'environnement globales pour le job
    env:
      DEBUG_MODE: ${{ github.event.inputs.debug_mode || 'false' }}
      ENGINE_SCRIPT: 'content_factory/auto_content_engine.py'
      DISABLE_SCRIPT: 'scripts/disable_youtube_uploader.py'
      OUTPUT_DIR: './output_results/'
      
    steps:
    
    # ⬇️ ÉTAPE 1: Initialisation
    # Nécessite un chemin 'path' pour checkout dans un conteneur
    - name: ⬇️ Checkout du code
      uses: actions/checkout@v4
      with:
        path: app # Le code sera cloné dans /app dans le conteneur
    
    # 📦 ÉTAPE 2: Installation et Mise à jour
    # Ces commandes sont exécutées dans le conteneur Python
    - name: 📦 Préparation de l'environnement
      working-directory: ./app # Définir le répertoire de travail dans le conteneur
      run: |
        echo "## Installation des dépendances Python"
        # S'assurer que pip et setuptools sont à jour pour éviter les problèmes
        python -m pip install --upgrade pip setuptools
        
        # Installer les dépendances du projet
        pip install -r requirements.txt
        
    # 🚀 ÉTAPE 3: Préparation et Lancement du Moteur
    - name: 🚀 Exécution du Workflow Python
      working-directory: ./app
      run: |
        echo "## Démarrage du processus de génération"
        echo "Mode Debug: ${{ env.DEBUG_MODE }}"
        
        # 3.1. Exécution conditionnelle du script de désactivation
        if [ -f "${{ env.DISABLE_SCRIPT }}" ]; then
          echo "Exécution de la désactivation..."
          python "${{ env.DISABLE_SCRIPT }}"
        else
          echo "⚠️ Avertissement: Script de désactivation non trouvé."
        fi
        
        # 3.2. Lancement du Moteur de Contenu
        if [ -f "${{ env.ENGINE_SCRIPT }}" ]; then
          echo "Lancement du Moteur : ${{ env.ENGINE_SCRIPT }}"
          
          # Exécution. Utilise 'python' car nous sommes dans un conteneur python:3.11
          DEBUG_MODE=${{ env.DEBUG_MODE }} python "${{ env.ENGINE_SCRIPT }}"
          
        else
          echo "❌ Erreur Critique : Fichier Moteur manquant (${{ env.ENGINE_SCRIPT }})."
          exit 1
        fi
        
    # 💾 ÉTAPE 4: Sauvegarde des résultats
    # L'artefact est uploadé depuis le chemin de travail du conteneur (/app/output_results/)
    - name: 💾 Sauvegarde des Artefacts de génération
      uses: actions/upload-artifact@v4
      if: always() 
      with:
        name: resultats-${{ github.run_id }}
        path: app/${{ env.OUTPUT_DIR }} # Chemin ajusté au conteneur
        retention-days: 7 
        
    # 🔔 ÉTAPE 5: Notifications
    - name: 🔔 Notification du Statut
      run: |
        if [[ $? -eq 0 ]]; then
          echo "🎉 Le workflow s'est terminé avec succès !"
        else
          echo "❌ Le workflow a échoué. Veuillez consulter les logs."
          exit 1 # Pour s'assurer que l'étape échoue si le run précédent a échoué
        fi
      # La condition 'always()' permet d'atteindre cette étape après l'upload, 
      # mais nous vérifions le statut du job avec $? (ou nous pourrions utiliser 'if: success()')
      # Pour cette version, je reviens à 'if: success()'/'if: failure()' pour plus de clarté
      if: always()
